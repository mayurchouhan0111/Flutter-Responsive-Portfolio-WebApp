import 'package:flutter/material.dart';

const primaryColor = Color(0xFF00D4FF); // A vibrant cyan-blue for accents
const secondaryColor = Color(0xFF1E1E2E); // A sleek dark blue-gray
const darkColor = Color(0xFF151521); // A deeper dark shade for backgrounds
const bodyTextColor = Color(0xFFD1D1E9); // Soft, readable pastel white-blue
const bgColor = Color(0xFF0F0F17); // A near-black with a hint of blue tint for depth
const defaultPadding = 20.0;
